case_id: I
expect:
  animation:
    max_approved: 3
  content:
    artifact_types:
    - slide_plan
    - pptx_deck
    banned_regex: []
    must_exist:
    - pptx_deck
  media:
    check_consistency: true
  plan:
    must_include_steps:
    - slide_plan
    - pptx
  ppt:
    max_dense_ratio: 0.5
    max_repeated_layout: 4
    require_notes: true
  routing:
    requested_outputs:
    - pptx
mode_targets:
- direct_codex
- builtin
- dynamic
- mixed
source: I_animation_and_ppt.yaml
title: Animation Necessity & PPT Quality（每页都加动画 / 讲义可编辑性）
